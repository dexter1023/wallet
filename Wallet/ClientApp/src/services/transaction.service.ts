import axios from '../plugins/axios'
import { ITransaction } from '../types/transaction.model'

export class TransactionService {
  async getServices(): Promise<ITransaction[]> {
    try {
    } catch (e) {
      throw e
    }
  }
}
